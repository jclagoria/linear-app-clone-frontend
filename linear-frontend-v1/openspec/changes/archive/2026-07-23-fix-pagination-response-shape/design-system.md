# Design System — Fix Pagination Response Shape

## Impact

No design system changes. This fix is scoped to the data layer (API response type and store destructuring). No UI components, layout tokens, typography, color semantics, or accessibility contracts are affected.

Refer to the main design system at `openspec/specs/frontend/ui-generic-components.md` for the existing component catalog.
