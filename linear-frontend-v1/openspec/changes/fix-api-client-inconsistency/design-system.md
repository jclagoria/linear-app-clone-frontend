# Design System — Fix: API Client Inconsistency

No design system changes. This change is purely infrastructural — no new UI components, no visual changes, no token updates.

All existing components remain unchanged. The fix operates entirely within the data layer (API client integration).
