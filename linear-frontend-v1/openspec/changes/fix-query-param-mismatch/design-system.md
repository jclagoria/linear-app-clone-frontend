# Design System — Fix: Query Parameter Mismatch

No design system changes. This change only alters API query parameter construction in the data layer. No new components, no visual changes, no component state changes, no a11y contract changes.

All existing design system tokens, components, and a11y contracts remain unchanged.
